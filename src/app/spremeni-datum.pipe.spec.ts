import { SpremeniDatumPipe } from './spremeni-datum.pipe';

describe('SpremeniDatumPipe', () => {
  it('create an instance', () => {
    const pipe = new SpremeniDatumPipe();
    expect(pipe).toBeTruthy();
  });
});
